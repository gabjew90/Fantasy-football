"""Sleeper Fantasy Auto-Manager — notification-only decision engine.

Watches the league so the user doesn't have to watch football: every decision
computed, delivered to the phone before its deadline with exactly one action
left (tap approve in the Sleeper app). Never writes to Sleeper.
"""
