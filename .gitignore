__pycache__/
*.pyc
.venv/
venv/
# Yahoo OAuth tokens — never commit
.yahoofantasy
.pytest_cache/
data/raw/*
!data/raw/.gitkeep
# processed outputs are regenerable; keep tiers.csv + board tracked for portability
data/processed/*.parquet
data/processed/*.json
# play-by-play draft logs are a personal record, not project source
data/logs/
.DS_Store
.env
data/manager/
reports/manager/

# Local TLS material for the engine bridge — machine-specific, never shared.
# The private key must not be in version control; regenerate with
# scripts/make_bridge_cert.sh
data/draftrig/tls/*.pem
data/draftrig/tiers_bakeoff_*.csv
data/draftrig/season_*.txt
data/draftrig/prerank*.json
node_modules/

# scratch the rig and the harness leave behind (review 2026-09-02)
data/draftrig/aside/
data/draftrig/*.log
data/processed/backtest/
data/processed/*.csv
data/processed/*.txt

# identity-harness reference boards (scripts/board_identity.py), regenerable
data/draftrig/ref_*.csv

# the positional missed-games table (scripts/derive_absence_bands.py) is a small tracked artifact
!data/processed/absence_bands.json
